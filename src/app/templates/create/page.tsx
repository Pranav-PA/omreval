import TemplateCreator from '@/components/TemplateCreator';

export const metadata = { title: 'New template — OMREval' };

export default function CreateTemplatePage() {
  return <TemplateCreator />;
}
